  445  helm install elasticsearch . -f values.yaml  --namespace observabilit
  446  helm install elasticsearch . -f values.yaml  --namespace observability
  447  helm uninstall elasticsearch --namespace observability
  448  helm install elasticsearch . -f values.yaml  --namespace observability
  452  helm uninstall elasticsearch --namespace observability
  453  helm install elasticsearch . -f values.yaml  --namespace observability
  455  helm uninstall elasticsearch --namespace observability
  456  helm install elasticsearch . -f values.yaml  --namespace observability
  458  helm uninstall elasticsearch --namespace observability
  459  helm install elasticsearch . -f values.yaml  --namespace observability
  460  helm install elasticsearch . -f values.yaml  --namespace observability
  574  helm search hazelcast
  575  helm search hub hazelcast
  576  helm search repo hazelcast
  577  helm repo list
  578  helm repo add hazelcast https://hazelcast-charts.s3.amazonaws.com/
  579  helm repo list
  580  helm repo update
  581  helm search repo hazelcast
  583  helm inspect values hazelcast/hazelcast
  593  helm search repo hazelcast
  594  helm install helm install hazelcast hazelcast/hazelcast --values=values.yaml --namespace=hazelcast
  595  helm install hazelcast hazelcast/hazelcast --values=values.yaml --namespace=hazelcast
  596  helm install hazelcast hazelcast/hazelcast --values=values.yaml --namespace=hazelcast
  597  helm install hazelcast hazelcast/hazelcast --values=values.yaml --namespace=hazelcast
  599  helm install hazelcast hazelcast/hazelcast --values=values.yaml --namespace=hazelcast
  656  helm search prometheus
  657  helm search repo prometheus
  658  helm search hub prometheus
  659  history | grep helm
  665  git clone  https://github.com/helm/charts.git
  673  helm install prometheus-server . -n monitoring
  674  helm install prometheus-server . --values=values.yaml -n monitoring
  675  helm install prometheus-server . --values=values.yaml -n monitoring
  676  helm install prometheus-server . --values=values.yaml -n monitoring
  679  git clone https://github.com/prometheus-community/helm-charts.git
  682  cd helm-charts/
  687  helm install prometheus-server . --values=values.yaml -n monitoring
  696  helm repo add stable https://charts.helm.sh/stable
  697  helm search kube-state-metrics
  698  helm search repo kube-state-metrics
  702  helm install kube-state-metrics . -n kube-system
  706  cd prometheus/helm-charts/charts/
  710  helm install prometheus-server --values=values.yaml . -n monitoring
  711  helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
  712  helm search repo prometheus-community
  716  helm install kube-prometheus-stack . --values=values.yaml -n monitoring
  718  helm search
  719  helm search repo
  720  helm search repo prometheus
  721  helm install kube-prometheus-stack prometheus-community/kube-prometheus-stack -n monitoring
  723  helm delete kube-prometheus-stack
  724  helm delete kube-prometheus-stack -n monitoring
  726  helm delete kube-prometheus-stack --values=values.yaml -n monitoring
  727  helm install kube-prometheus-stack prometheus-community/kube-prometheus-stack --values=values.yaml -n monitoring
  729  helm delete kube-prometheus-stack -n monitoring
  730  helm repo update
  732  helm install prometheus stable/prometheus --nmespace monitoring
  733  helm install prometheus stable/prometheus -n monitoring
  736  helm delete prometheus -n monitoring
  737  helm install kube-prometheus-stack prometheus-community/kube-prometheus-stack --values=values.yaml -n monitoring
  748  git clone https://github.com/jaegertracing/helm-charts.git
  750  cd helm-charts/
  754  helm repo add jaegertracing https://jaegertracing.github.io/helm-charts
  755  helm search repo jaeger
  756  helm show values jaegertracing/jaeger
  763  helm install tracing . --values=values.yaml -n tracing
  764  helm install tracing . --values=values.yaml -n tracing
  765  helm install tracing . --values=values.yaml -n tracing
  766  helm install tracing . --values=values.yaml -n tracing
  767  helm install tracing . --values=values.yaml -n tracing
  768  helm install tracing jaegertracing/jaeger --values=values.yaml -n tracing
  793  helm repo add elastic https://helm.elastic.co
  794  helm search repo elastic
  799  helm delete tracing -n tracing
  803  helm search repo fluentbit
  804  history | grep helm add
  805  history | grep 'helm add'
  807  history | rep helm
  808  history | grep helm
  809  helm repo list
  810  helm search repo fluent
  814  helm install fluent-bit stable/fluent-bit --values=values.yaml -n fluentbit-system
  815  helm install fluent-bit stable/fluent-bit --values=values.yaml -n fluentbit-system
  816  helm install fluent-bit stable/fluent-bit --values=values.yaml -n fluentbit-system
  817  helm install fluent-bit stable/fluent-bit --values=values.yaml -n fluentbit-system
  818  helm delete fluent-bit -n fluetbit-system
  819  helm delete fluent-bit -n fluentbit-system
  820  helm install fluent-bit stable/fluent-bit --values=values.yaml -n fluentbit-system
  823  helm search repo lastic
  824  helm search repo elastic
  825  helm repo add elastic https://helm.elastic.co
  826  helm show values elastic/elasticsearch
  830  helm install elasticsearch elastic/elasticsearch --values=values.yaml -n logging
  831  helm delete elasticsearch -n logging
  832  helm install elasticsearch elastic/elasticsearch --values=values.yaml -n logging
  833  helm delete elasticsearch -n logging
  834  helm install elasticsearch elastic/elasticsearch --values=values.yaml -n logging
  835  helm delete fluent-bit -n fluentbit-system
  838  helm install fluent-bit stable/fluent-bit --values=values.yaml -n fluentbit-system
  839  helm search repo jaegertracing/jaeger
  844  cd helm-charts/
  852  helm install jaeger jaegertracing/jaeger --values=values.yaml -n tracing
  853  helm delete jaeger -n tracing
  854  helm install jaeger jaegertracing/jaeger --values=values.yaml -n tracing
  857  helm search repo elastic
  858  helm search repo jaeger
  859  helm delete jaeger -n tracing
  860  helm install jaeger jaegertracing/jaeger --values=values.yaml -n tracing
  861  helm delete jaeger -n tracing
  862  helm install jaeger jaegertracing/jaeger --values=values.yaml -n tracing
  863  helm delete elasticsearch -n logging
  867  helm install elasticsearch elastic/elasticsearch --values=values.yaml -n logging
  870  cd helm-charts/
  874  helm delete jaeger -n tracing
  875  helm install jaeger jaegertracing/jaeger --values=values.yaml -n tracing
  896  history | grep helm
  900  history | grep helm
  901  helm delete elasticsearch -n logging
  902  helm install elasticsearch elastic/elasticsearch --values=values.yaml -n logging
  915  cd helm-charts/charts/jaeger
  916  history | helm
  917  history | grep helm
  918  helm delete jaeger -n tracing
  919  helm install jaeger jaegertracing/jaeger --values=values.yaml -n tracing
  924  helm repo add hashicorp https://helm.releases.hashicorp.com
  925  helm search repo hashicorp/vault
  929  git clone https://github.com/hashicorp/vault-helm.git
  931  cd vault-helm/
  933  #helm install vault hashicorp/vault --values=values.yml
  935  helm install vault hashicorp/vault --values=values.yml -n vault
  937  helm install vault hashicorp/vault --values=values.yaml -n vault
  938  helm status vault
  939  helm status vault -n vault
  940  helm get manifest vault -n vault
  943  history | grep helm >> b.txt
